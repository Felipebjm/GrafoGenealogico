<Window x:Class="InterfazGrafica.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Árbol genealógico" Height="600" Width="950">
    <Grid>
        <!-- Se definen 2 filas: header y contenido -->
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <!-- header -->
            <RowDefinition Height="*"/>
            <!-- resto -->
        </Grid.RowDefinitions>

        <!-- Se define 2 columnas en la fila de contenido: menu y contenido -->
        <Grid.ColumnDefinitions>
            <!-- esto aplica a toda la ventana, pero se usa solo en la fila 1 -->
            <!-- menu lateral -->
            <ColumnDefinition Width="200"/>
            <!--Contenido-->
            <ColumnDefinition Width="31*"/>
            <ColumnDefinition Width="19*"/>
            
        </Grid.ColumnDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="#1F2937">
            <!-- DockPanel con el textbox -->
            <DockPanel Margin="10,0">
                <TextBlock Text="Árbol genealógico"
                           Foreground="White"
                           FontSize="18"
                           VerticalAlignment="Center"
                           FontWeight="Bold"/>
            </DockPanel>
        </Border>

        <!-- MENU LATERAL -->
        <StackPanel Grid.Row="1" Grid.Column="0" Background="#F3F4F6">
            <TextBlock Text="Menú" Margin="10" FontWeight="Bold"/>

            <Button x:Name="BtnAgregar"
                    Content="Agregar familiar"
                    Margin="10,5"
                    Padding="5"
                    Click="BtnAgregar_Click"/>

            <Button x:Name="BtnMapa"
                    Content="Mapa"
                    Margin="10,5"
                    Padding="5"
                    Click="BtnMapa_Click"/>

            <Button x:Name="BtnEstadisticas"
                    Content="Estadísticas"
                    Margin="10,5"
                    Padding="5"
                    Click="BtnEstadisticas_Click"/>
        </StackPanel>

        <!-- ÁREA DE CONTENIDO -->
        <ContentControl x:Name="MainContent"
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="10,10,10,10" Grid.ColumnSpan="2"/>
    </Grid>
</Window>

